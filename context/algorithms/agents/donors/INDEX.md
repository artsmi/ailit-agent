# Donor index (Agents pipeline)

Сводка по донорам для пакета **agents** без копирования кода и без ссылок на артефакты pipeline (CR4). Сопоставления migrate / do-not-migrate — в [`../donor-comparison.md`](../donor-comparison.md).

## Taken

Архитектурные решения из сравнения с **claude-code**, **opencode**, **ruflo**, **superpowers** перенесены в текст [`../donor-comparison.md`](../donor-comparison.md), [`../killer-features.md`](../killer-features.md) и связанные файлы пакета.

## Rejected

Явные отказные решения описаны в матрице «донор vs ailit» там, где строка требует **do-not-migrate**; этот INDEX не дублирует таблицу целиком.

## Not researched

Репозитории из `project-workflow.mdc`, не упомянутые в [`../donor-comparison.md`](../donor-comparison.md), считаются вне scope текущего канона до отдельного исследования **`101`**.
