# Donor index (AgentMemory)

Учёт внешних репозиториев для темы **AgentMemory** без путей `context/artifacts/…` (CR4). Нормативное поведение — в файлах пакета и [`../INDEX.md`](../INDEX.md).

## Taken

Идеи циклов агента, инструментов и компактации из сравнений с внешними продуктами отражены в разделах runtime, LLM-команд и observability пакета (см. глоссарий и `failure-retry-observability.md`).

## Rejected

Не фиксировалось в отдельном donor-index до введения `donors/`; дальнейшие отклонения документирует **`104`** при обновлениях канона.

## Not researched

Отдельные репозитории из таблицы `project-workflow.mdc` могли не входить в scope исследований для утверждённого пакета; новый target-doc обновляет эту секцию по волнам **`103` → `101`**.
