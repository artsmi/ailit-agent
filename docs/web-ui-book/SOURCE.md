# Происхождение материалов

Содержимое каталога `docs/web-ui-book/` (кроме этого файла и `INDEX.md`) — зеркало статического экспорта Stitch:

- **Источник:** `/home/artem/Desktop/айлит/stitch_example_showcase_system/`
- **Синхронизация в репозиторий:** 2026-05-05

При обновлении брендбука на стороне дизайна скопируйте заново дерево из актуального каталога stitch в `docs/web-ui-book/` и обновите дату в этом файле.

**Зависимости HTML:** страницы используют Tailwind CDN, Google Fonts и аналоги — без сети внешний вид может деградировать. Политика vendoring — отдельная задача `start-feature`.
