---
description: Примеры и антипримеры для 13_tech_writer — context/*, INDEX.md, memories, report
alwaysApply: false
---

# Примеры Technical Writer (13)

Этот файл дополняет `tech-writer-process.mdc` короткими good/bad examples. Если пример расходится с `artifact-tech-writer-report.mdc`, **artifact schema wins**.

## Good: `context/arch`

```markdown
# Runtime Supervisor

## Назначение
User-level процесс, который держит runtime socket и обслуживает CLI/Desktop клиентов.

## Точки входа
- `agent_core/runtime/supervisor.py` — запуск процесса и lifecycle.
- `scripts/install` — установка `systemd --user` unit.

## Входящие данные
- CLI commands через runtime socket.
- Desktop bridge requests.

## Диагностика
- `ailit runtime status`
- `journalctl --user -u ailit.service -f`

## Что проверять при изменениях
- e2e readiness
- socket path isolation
- install/update service flow
```

Почему хорошо: есть границы, entrypoints, входящие данные, диагностика и влияние изменений.

## Bad: `context/arch`

```markdown
Runtime отвечает за запуск. В нём есть разные файлы. Всё работает через socket.
```

Проблемы: нет границ, entrypoints, диагностики, влияния изменений.

## Good: `context/proto`

```markdown
# Runtime Socket Protocol

## Участники
- CLI client: `agent_core/runtime/client.py`.
- Runtime supervisor: `agent_core/runtime/supervisor.py`.

## Transport
Unix socket в `AILIT_RUNTIME_DIR`.

## Messages
- `status`: required `request_id`, returns `state`, `pid`, `ready`.
- `shutdown`: required `request_id`, returns `accepted`.

## Ошибки и деградация
- Missing socket возвращает client-side unavailable error.
- Timeout диагностируется через `ailit runtime status`.
```

## Bad: `context/proto`

```markdown
CLI общается с runtime через сокет. Есть status и shutdown.
```

Проблемы: нет участников, формата payload, ошибок, entrypoints.

## Good: `context/start`

```markdown
# Runtime Start

## Local development
- `.venv/bin/python -m agent_core.runtime.supervisor` — direct supervisor start.

## Environment
- `AILIT_RUNTIME_DIR` — socket/state dir; required for isolated tests.
- `AILIT_CONFIG_DIR` — config root; defaults documented in project config loader.

## Readiness
- `ailit runtime status` checks socket availability and supervisor state.
```

## Bad: `context/start`

```markdown
Запуск через python или CLI, переменные окружения стандартные.
```

Проблемы: нет команд, required env, readiness и источников.

## Good: `context/tests`

```markdown
# Runtime Tests

## Unit
- `.venv/bin/python -m pytest tests/runtime` — runtime client/supervisor behavior.

## E2E
- `.venv/bin/python -m pytest tests/e2e/test_runtime_cli.py` — CLI readiness and isolated env.

## Isolation
- `tests/conftest.py` sets `AILIT_RUNTIME_DIR`, `AILIT_CONFIG_DIR`, `AILIT_STATE_DIR`.
```

## Bad: `context/tests`

```markdown
Тесты запускаются pytest. Есть unit и e2e.
```

Проблемы: нет entrypoints, команд, связи с изоляцией и подсистемами.

## Good: memory entry

```markdown
# Feature: runtime socket lifecycle

## Что изменилось
- Runtime supervisor теперь описывает readiness и shutdown через единый socket protocol.
- CLI status command получил обновлённый payload с `ready` и `pid`.

## Затронутые процессы
- Runtime supervisor.
- CLI client.

## Риски
- Следить за согласованностью socket payload в `context/proto/runtime-socket.md` и CLI tests.
```

## Bad: memory entry

```markdown
# Работы завершены

Сделали много правок в агентах, всё стало лучше.
```

Проблемы: нет фактов, затронутых модулей, поведения, тестов, рисков и дальнейших точек внимания.

## Good: `INDEX.md`

```markdown
# Context Index

- `runtime-supervisor.md` — процесс runtime supervisor; читать при изменениях socket lifecycle, install и readiness.
- `desktop-bridge.md` — bridge UI/runtime; читать при изменениях desktop событий.
```

## Bad: `INDEX.md`

```markdown
# Index

- runtime
- desktop
```

Проблемы: нет путей, ролей файлов и подсказки, когда читать.

## Tech Writer Report Template

```markdown
# Tech Writer Report

## Режим
- feature

## Входной inventory
- Inventory: `{artifacts_dir}/change_inventory.md`

## Создано
- `context/arch/<file>.md` — <роль>

## Изменено
- `context/proto/<file>.md` — <что обновлено>
- `context/memories/index.md` — добавлена запись памяти

## Обновлённые INDEX.md
- `context/proto/INDEX.md`
- `context/memories/index.md`

## Не изменялось
- `context/start/` — запуск не менялся, подтверждено inventory section 5

## Допущения и пробелы
- <если есть>

## Selective sync hints
- `context/arch/<file>.md`
```

## Anti-Patterns

- Не переписывай несвязанные knowledge sections.
- Не превращай `context/*` в пересказ diff.
- Не записывай непроверенные гипотезы как факт.
- Не меняй product code.
- Не обновляй `.cursor/rules/project/**`, кроме разрешённых полей `project-config.mdc` в learn-сценарии, если stage rules это требуют.
- Не записывай raw prompts, chain-of-thought, secrets, большие logs или pipeline events в `context/memories/`.
- Не подменяй canonical files локальным DB index или selective sync metadata.

## Checklist

- [ ] `change_inventory.md` прочитан как основной вход.
- [ ] Обновлены только затронутые sections.
- [ ] `INDEX.md` обновлены.
- [ ] Memory entry создана или причина отсутствия записана в report.
- [ ] `tech_writer_report.md` перечисляет created/modified/indexes/unchanged/gaps.
- [ ] Selective sync hints указаны.
- [ ] Product code, тесты и project rules не менялись вне разрешённого `project-config.mdc` в learn.
