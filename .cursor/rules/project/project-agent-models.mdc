---
description: Выбор моделей Cursor Subagents для агентов проекта
alwaysApply: false
---

# Модели агентов

## Политика репозитория ailit-agent

Роли `02+`, роли **A–B–C–M** (`02A`…`07M`) и target-doc роли `100`–`108` запускаются **только как Cursor Subagents** с моделью **строго по этой карте**. Модель чата, в котором сидит `01`/`100`, **не** задаёт модель Subagent и **не** разрешает подставлять slug из подсказок среды.

`Auto` в карте — это **фиксированное значение карты**: специальный режим Cursor Subagents (не «любая модель по усмотрению рантайма из списка дорогих»). Для строки с `Auto` оркестратор **не** передаёт параметр `model` в вызов Subagent; любой явный `model` при значении карты `Auto` — **ошибка политики** и сжигает квоту непредсказуемо.

**Запрещено без явного изменения этой карты человеком:**

- передавать в Subagent/Task параметр `model` со slug вроде `gpt-5.5-medium`, `claude-opus-*`, `composer-*` и т.п.;
- выбирать модель из списка «доступных моделей» в system rules вместо строки карты;
- «ускорять» pipeline дорогой моделью, если в карте для роли указано `Auto`.

Если файл отсутствует или строка роли не парсится — оркестратор останавливает pipeline (см. правила валидации ниже). В этом репозитории файл **закреплён**; не полагаться на fallback «как в IDE».

## Строгий формат

Используй только строки вида `- \`NN_agent_name\`: \`model\``.

## Карта моделей

- `01_orchestrator`: `Auto`
- `02_analyst`: `Auto`
- `03_tz_reviewer`: `Auto`
- `04_architect`: `Auto`
- `05_architecture_reviewer`: `Auto`
- `06_planner`: `Auto`
- `07_plan_reviewer`: `Auto`
- `08_developer`: `Auto`
- `09_code_reviewer`: `Auto`
- `11_test_runner`: `Auto`
- `12_change_inventory`: `Auto`
- `13_tech_writer`: `Auto`
- `100_target_doc_orchestrator`: `Auto`
- `101_donor_researcher`: `Auto`
- `102_current_repo_researcher`: `Auto`
- `103_target_doc_synthesizer`: `Auto`
- `104_target_doc_author`: `Auto`
- `105_target_doc_verifier`: `Auto`
- `106_implementation_plan_author`: `Auto`
- `107_implementation_plan_reviewer`: `Auto`
- `108_target_doc_reader_reviewer`: `Auto`

Явный slug модели в карте допускается **только** после явного решения человека и правки этого файла; иначе по умолчанию везде `Auto`.

## Правила валидации

- Не переименовывай ключи агентов и не добавляй новые вне **карты ниже** (включая роли `02A`…`07M`, `01`, `02`–`09`, `11`–`13` и target-doc `100`…`108`).
- Значение модели — либо точно `Auto`, либо точная строка модели для Cursor Subagent, согласованная человеком в этой карте.
- Не выполняй внешнюю проверку моделей. Если явно заданная модель недоступна Cursor Subagent runtime или не входит в разрешённый список текущей среды, оркестратор обязан остановить pipeline и вынести пользователю блокирующий вопрос.
- Если файл не парсится по формату, содержит неизвестный ключ или модель недоступна / не проходит проверку, оркестратор обязан остановить pipeline и вынести пользователю блокирующий вопрос.
