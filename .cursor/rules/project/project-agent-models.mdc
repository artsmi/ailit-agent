---
description: Выбор моделей Cursor Subagents для агентов проекта
alwaysApply: false
---

# Модели агентов

Этот файл **опционален**. Если файл отсутствует или конкретный агент не указан, оркестратор использует `auto`.

Используй явные модели только там, где они действительно окупаются. Обычно это архитектура и код (`04`, `05`, `08`, `09`). Для оркестрации, review-маршрутизации, инвентаризации и записи контекста сначала оставляй `Auto`.

## Строгий формат

Используй только строки вида `- \`NN_agent_name\`: \`model\``.

## Карта моделей

- `01_orchestrator`: `Auto`
- `02_analyst`: `Auto`
- `03_tz_reviewer`: `Auto`
- `04_architect`: `Auto`
- `05_architecture_reviewer`: `Auto`
- `06_planner`: `Auto`
- `07_plan_reviewer`: `Auto`
- `08_developer`: `Auto`
- `09_code_reviewer`: `Auto`
- `10_researcher`: `Auto`
- `11_test_runner`: `Auto`
- `12_change_inventory`: `Auto`
- `13_tech_writer`: `Auto`
- `14_donor_researcher`: `Auto`
- `15_research_synthesizer`: `Auto`
- `16_plan_author`: `Auto`
- `17_research_plan_reviewer`: `Auto`

## Правила валидации

- Не переименовывай ключи агентов и не добавляй новые вне списка `01`-`17`.
- Значение модели — точная строка модели для Cursor Subagent.
- Не выполняй внешнюю проверку моделей. Если явно заданная модель недоступна Cursor Subagent runtime или не входит в разрешённый список текущей среды, оркестратор обязан остановить pipeline и вынести пользователю блокирующий вопрос.
- Если файл не парсится по формату, содержит неизвестный ключ или модель недоступна / не проходит проверку, оркестратор обязан остановить pipeline и вынести пользователю блокирующий вопрос.
